language: c
dist: xenial
script: mkdir build && cd build && cmake .. && make && ./duskc ../examples/main.ds
compiler:
  - clang
  - gcc
os:
  - linux
  - osx
addons:
  apt:
    sources:
      - george-edison55-precise-backports
    packages:
      - cmake-data
      - cmake
