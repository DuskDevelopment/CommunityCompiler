language: cpp
script: mkdir build && cd build && cmake .. && (make || nmake)
compiler:
  - clang
  - gcc
os:
  - linux
  - windows
  - osx
addons:
  apt:
    sources:
      - george-edison55-precise-backports
    packages:
      - cmake-data
      - cmake
